# -----------------------------------------------
# Bug Prioritization Engine — environment config
# Copy to .env and fill in your values.
# NEVER commit .env to version control.
# -----------------------------------------------

# Jira connection
JIRA_URL=https://yourorg.atlassian.net
JIRA_EMAIL=your-email@example.com
JIRA_API_TOKEN=your-jira-api-token
JIRA_PROJECT_KEY=MYPROJ

# Model storage
MODEL_PATH=models/artifacts

# API security
RETRAIN_API_KEY=change-me-to-a-long-random-secret

# Cache (seconds)
CACHE_TTL_SECONDS=300

# Sentence-transformers model
EMBEDDING_MODEL=all-MiniLM-L6-v2

# Scheduler (hour 0-23 UTC)
RETRAIN_CRON_HOUR=2
RETRAIN_LOOKBACK_DAYS=90

# Logging
LOG_LEVEL=INFO

# Optional: MLflow tracking (leave blank to disable)
# MLFLOW_TRACKING_URI=http://localhost:5000
